//Madeleine Allen
//code to test the trees and stuff
#include "cards.h"
#include<iostream>
#include<string>
using namespace std;

//creates two trees (one tree empty) and does tests


//bool insert(string value)
//{
  //      if(!root) //empty tree
    //            //replace with == after overloading
      //  {
       //         root = new Node(value);
        //        return true;
      //  }
//        return insert(value, root);
//}


//DONT HAVE TO MAKE TEST CODE INTERACTIVE (ACTUALLY DONT DO THAT)
//LITERALLY JUST PUT FUNCTIONS IN HERE	
	CardList *a;
	//insert data into alice
	//Node *n = a.root
a.insert("h 3", a.root);
a.insert("s 10", a.root);
a.insert("c a", a.root);
a.insert("c 3", a.root);
a.insert("s 5", a.root);
a.insert("h 10", a.root);
a.insert("d a", a.root);

	//let user choose all or one of the tests
	//print and test methods for alice
	//cout << "CardList: " <<endl << "print: ";
	//alice.print();
	//add it here on other computer	
a.print();
